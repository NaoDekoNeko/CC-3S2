import org.junit.jupiter.api.Test;
import static org.assertj.core.api.Assertions.assertThat;

public class pipipi {
    @Test
    public void Test1(){
        var calc = new Calculadora();
        var resultado = calc.suma(10,15);
        assertThat(resultado).AreEqual()
    }
}
